import React from 'react';
// import PropTypes from 'prop-types';

const NotFound = () => {

    return (
        <h1>Sorry, page don't exist</h1>
    );
};

// NotFound.propTypes = {};

export default NotFound
